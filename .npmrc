# Security settings
engine-strict=true

# Prevent accidental publish
publish=false

# Use exact versions when adding new packages
save-exact=true

# Audit on install
audit=true

# Prevent scripts from running during install (can enable per-package if needed)
# ignore-scripts=true

# Registry (use default npm registry)
registry=https://registry.npmjs.org/
